# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# venv / uv
.venv/
venv/
env/
.uv-cache/

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/coverage/

# Static bundle (generated by frontend build, copied into wheel).
# Keep the directory itself so hatchling's force-include resolves at build
# time even before the SPA has been built.
src/pd_prep_for_pgdp/static/*
!src/pd_prep_for_pgdp/static/.gitkeep

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Local data / projects
pgdp-projects/
state.db*
*.sqlite*

# Env files
.env
.env.local
.env.*.local

# Logs
*.log

# Models cache
.cache/

# Claude Code session state (per-machine)
.claude/

# ship-issue bot temp cache (issue JSON written by ship-issue-pick.py)
.ship-issue-tmp/

# Hourly-regenerated spec-chain dashboard — workspace-only artifact, not source.
docs/spec-chain-report.md

# AI=1 make output capture log
.ci-ai.log
.worktrees/
.venv
frontend/node_modules
.venv
.worktrees/**/.venv
.worktrees/**/frontend/node_modules
